# App senter tidak memakai refleksi, serialisasi, atau JNI — aturan default sudah cukup.
# Compose & Kotlin sudah ditangani oleh consumer rules masing-masing library.
# Tidak ada SDK pihak ketiga (iklan/analitik) sehingga tidak ada keep-rule tambahan.
